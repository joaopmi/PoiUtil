# PoiUtil
Versão 2.0

Crie excel\n (.xlsx) passando Strings como localizações das células. Ex: "A1","AF:B3"

Exemplo no main na classe, apenas substitua o path em FileOutputStream ->
final FileOutputStream fos = new FileOutputStream(new File(path));
